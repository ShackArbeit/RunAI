{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "set PYTHONDONTWRITEBYTECODE=1 && python -B -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "python -m compileall app",
    "lint": "python -m ruff check .",
    "test": "python -m pytest"
  }
}
